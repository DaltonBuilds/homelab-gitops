apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: alertmanager-lab
  namespace: monitoring
spec:
  ingressClassName: traefik
  rules:
    - host: alertmanager.daltonbuilds.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monitoring-kube-prometheus-alertmanager
                port:
                  number: 9093
